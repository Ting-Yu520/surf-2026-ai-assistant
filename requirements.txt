# SURF-2026 AI Tactical Assistant — Python 依赖

# Core
streamlit>=1.32.0

# LLM API (Anthropic 兼容格式 → DeepSeek V4)
anthropic>=0.100.0

# VLM — migrated to DeepSeek via Anthropic SDK (2026-06-30)
# google-genai removed — no longer needed

# TTS (Edge TTS, 免费)
edge-tts>=6.1.0

# Video Processing
moviepy>=2.0.0

# Video Downloading (corner kick dataset)
yt-dlp>=2025.1.0

# Data & Validation
pydantic>=2.7.0
jsonschema>=4.22.0

# Utils
python-dotenv>=1.0.1
loguru>=0.7.2
Pillow>=10.0.0

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
